(function($, window) {
    $(function() {
      // ここに処理を書きます

      $('#date_picker') .flatpickr({"locale": "ja"});
      $('#date') .flatpickr({"locale": "ja"});
      
    });
  })(jQuery, window);